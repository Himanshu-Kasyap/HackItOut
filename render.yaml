services:
  - type: web
    name: climatelens
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: cd backend && python app.py
    envVars:
      - key: PORT
        value: 10000
